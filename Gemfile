source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

group :development do
  gem 'debug'
  gem 'irb'
  gem 'progressbar'
  gem 'rubocop'
end

group :test do
  gem 'minitest'
end
